# AndeStay Sanity Studio (standalone)

Project ID: `nnfu4wmn`  
Dataset: `production`

## Run locally

```bash
cd studio
npm install
npm run dev
```

Studio opens at http://localhost:3333

## Deploy

```bash
cd studio
npm run deploy
```

Schemas live in `schemaTypes/` (added in follow-up steps).
