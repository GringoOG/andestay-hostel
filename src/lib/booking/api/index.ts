/**
 * Booking API contract types (no runtime yet).
 * Future route handlers: /api/availability, /api/bookings, /api/payments, …
 */

export type {
  AvailabilityRequest,
  AvailabilityResponse,
  AvailabilityRoomQuote,
  BookingGuest,
  BookingRequest,
  BookingResponse,
  BookingStatus,
  GuestCount,
  IsoDate,
  PaymentRequest,
  PaymentResponse,
  PaymentStatus,
  StayDates,
} from "./types";
